package kr.co.mobpa.appbox.sdkSample

/**
 * 샘플을 실행하기 위해 바꿔야 하는 값입니다.
 *
 * **이 파일 한 곳만 고치면 됩니다.** Kotlin 구성과 Java 구성이 모두 여기를 참조하므로
 * 두 언어의 파일을 따로 수정할 필요가 없습니다.
 *
 * 두 값 모두 AppBox 콘솔에서 확인합니다.
 */
object SampleConfig {

    /** 콘솔에서 확인한 프로젝트 ID 입니다. */
    const val PROJECT_ID: String = "PROJECT_ID"

    /** 앱으로 패키징할 웹 주소입니다. */
    const val BASE_URL: String = "https://www.example.com"
}
